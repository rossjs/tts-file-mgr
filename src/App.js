import Typography from '@material-ui/core/Typography';
import FileManager from './components/FileManager';

const App = () => (
  <div className="App">
    <header className="App-header">
      <Typography>TTS File Manager</Typography>
    </header>
    <FileManager />
  </div>
);

export default App;
